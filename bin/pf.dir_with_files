#!/bin/ksh
# @Object: Echo the include path trimmed from empty dirs
# @Author: S.Chamberland
# @Date:   March 2014

newline='\n'
if [[ x$1 == x-n ]] ; then
   newline=' '
   shift
fi
VPATH=${1:-.}
for item in $(find $VPATH -type f) ; do echo ${item%/*} ; done | sort -u | tr '\n' "$newline"
