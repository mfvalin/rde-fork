#!/bin/ksh
DESC='Purple Frog main program'
USAGE="USAGE: ${MYSELF} TOOL [OPTIONS]"

prefix=${0##*/}

if [[ x"$(which $prefix$1 2>/dev/null)" != x && x"$1" != x ]] ; then
   $prefix$*
else
   ${prefix}help
fi
