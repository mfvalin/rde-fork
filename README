==== Before Tagging

Create/update "setup" file: etc/ssm/VERSION.sh

==== Installation

SSM_X=x/

BASE_INSTALL_DIR=/users/dor/armn/env/SsmBundles/ENV/d/${SSM_X}rde
BASE_SSM_BNDL_DIR=/users/dor/armn/env/SsmBundles/ENV/${SSM_X}rde

VERSION0=${SSM_X}rde_1.0.b8

VERSIONGIT=${VERSION0##*/}
VERSION=${VERSION0##*_}

cd $BASE_INSTALL_DIR
git clone ssh://localhost/users/dor/armn/env/GitDepots/rde.git $VERSION
cd $VERSION
git checkout -b tmp $VERSIONGIT
rm -rf .git

cd $BASE_SSM_BNDL_DIR
cp $BASE_INSTALL_DIR/$VERSION/etc/ssm/${VERSION}.sh .
